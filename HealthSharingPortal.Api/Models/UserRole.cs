namespace HealthSharingPortal.Api.Models
{
    public enum UserRole
    {
        Sharer, // Person sharing data
        HealthProvider, // Person providing medical services
        Researcher, // Person using data for research
        Moderator, // Person managing the health sharing portal
    }
}
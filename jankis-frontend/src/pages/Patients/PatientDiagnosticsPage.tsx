import React from 'react';

interface PatientDiagnosticsPageProps {}

export const PatientDiagnosticsPage = (props: PatientDiagnosticsPageProps) => {

    return (
        <h1>PatientDiagnosticsPage</h1>
    );

}